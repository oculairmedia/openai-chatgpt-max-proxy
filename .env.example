# Anthropic Claude Max Proxy Configuration
# Copy this file to .env and customize as needed:
#   cp .env.example .env

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================

# Port to run the proxy server on
PORT=8081

# Logging level (debug, info, warning, error, critical)
LOG_LEVEL=info

# Bind address for the server
# Use 0.0.0.0 to listen on all interfaces (default)
# Use 127.0.0.1 to listen only on localhost
BIND_ADDRESS=0.0.0.0

# ============================================================================
# MODEL CONFIGURATION
# ============================================================================

# Default Claude model to use
DEFAULT_MODEL=claude-sonnet-4-5-20250929

# ============================================================================
# API TIMEOUT CONFIGURATION
# ============================================================================
# Industry-standard timeout values in seconds

# Connection timeout: Time to establish TCP connection (5-10s is standard)
CONNECT_TIMEOUT=10.0

# Read timeout: Time between data chunks - detects stalled streams
READ_TIMEOUT=60.0

# Request timeout: Total timeout for non-streaming requests (2 minutes)
REQUEST_TIMEOUT=120.0

# Stream timeout: Total timeout for streaming requests (10 minutes)
# LLM generations can take longer, especially with extended thinking
STREAM_TIMEOUT=600.0

# ============================================================================
# DEBUG CONFIGURATION
# ============================================================================

# Enable stream tracing for debugging (true/false)
# WARNING: Trace files may contain sensitive content
STREAM_TRACE_ENABLED=false

# Directory to store stream trace files
STREAM_TRACE_DIR=stream_traces

# Maximum size of trace files in bytes (default: 256KB)
STREAM_TRACE_MAX_BYTES=262144

# ============================================================================
# STORAGE CONFIGURATION
# ============================================================================

# Path to store OAuth tokens
# Supports ~ for home directory expansion
TOKEN_FILE=~/.anthropic-claude-max-proxy/tokens.json

# ============================================================================
# OAUTH TOKEN (for headless mode)
# ============================================================================

# Long-term OAuth token for headless/production deployments
# Generate using: python cli.py --setup-token
# ANTHROPIC_OAUTH_TOKEN=sk-ant-oat01-...
